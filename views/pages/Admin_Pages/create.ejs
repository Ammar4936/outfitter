<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Create Product</title>
    <style>
        .parentContainer {
            display: flex;
            justify-content: center;
            align-items: center;
        }
        body {
            font-family: Arial, sans-serif;
            background-color: #f9f9f9;
            margin: 0;
            padding: 0;
            height: 100vh;
        }

        .form-container {
            background-color: #fff;
            padding: 30px;
            border-radius: 8px;
            box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
            width: 100%;
            max-width: 400px;
        }

        /* Heading Box Styling */
        .heading-box {
            background-color: #17a2b8; /* Same color as the layout */
            color: white;
            padding: 10px;
            font-size: 24px;
            font-weight: 600;
            text-align: center;
            margin-bottom: 20px;
            border-radius: 6px;
        }

        form label {
            display: block;
            margin-bottom: 8px;
            font-weight: bold;
            color: #555;
        }

        form input[type="text"],
        form input[type="number"],
        form input[type="file"],
        form textarea {
            width: 100%;
            padding: 10px;
            margin-bottom: 15px;
            border: 1px solid #ccc;
            border-radius: 4px;
            font-size: 16px;
            box-sizing: border-box;
        }

        form input[type="checkbox"] {
            margin-right: 8px;
        }

        button {
            display: block;
            width: 100%;
            padding: 10px;
            background-color: #17a2b8; /* Same color as the layout */
            color: white;
            border: none;
            border-radius: 4px;
            font-size: 16px;
            cursor: pointer;
            text-transform: uppercase;
            margin-top: 15px;
        }

        button:hover {
            background-color: #17a2b8;
        }

        /* Back Button Styling */
        .back-button {
            display: inline-block;
            margin-top: 20px;
            text-align: center;
            color: #17a2b8;
            font-size: 16px;
            text-decoration: none;
            font-weight: 600;
        }

        .back-button:hover {
            text-decoration: underline;
        }
    </style>
</head>
<body>
    <div class="parentContainer">
        <div class="form-container">
            <!-- Create Product Heading inside a Box -->
            <div class="heading-box">
                Create Product
            </div>

            <form id="itemForm" method="post" enctype="multipart/form-data" action="/admin/products/create">
                <label for="title">Title</label>
                <input type="text" id="title" name="title" placeholder="Enter the title" required>

                <label for="description">Description</label>
                <textarea id="description" name="description" rows="4" placeholder="Enter the description" required></textarea>

                <label for="price">Price</label>
                <input type="number" id="price" name="price" step="0.01" placeholder="Enter the price" required>

                <label for="isFeatured">
                    <input type="checkbox" id="isFeatured" name="isFeatured">
                    Is Featured
                </label>

                <label for="imageSrc">Product Image</label>
                <input type="file" id="imageSrc" name="imageSrc" accept="image/*" required>

                <button type="submit">Submit</button>
            </form>

            <!-- Back Button -->
            <a href="/admin/products" class="back-button">Back to Products</a>
        </div>
    </div>
</body>
</html>
