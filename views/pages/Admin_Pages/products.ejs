<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Product Management</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        body {
            background-color: #f4f6f9;
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
        }
        .container {
            background-color: white;
            border-radius: 12px;
            box-shadow: 0 4px 6px rgba(0,0,0,0.1);
            padding: 30px;
            margin-top: 30px;
        }
        .table {
            margin-top: 20px;
        }
        .table thead {
            background-color: #f8f9fa;
            color: #343a40;
        }
        .table th {
            font-weight: 600;
            text-transform: uppercase;
            letter-spacing: 1px;
            border-bottom: 2px solid #dee2e6;
        }
        .table tr:hover {
            background-color: #f1f3f5;
            transition: background-color 0.3s ease;
        }
        .btn-create {
            background-color: #28a745;
            color: white;
            border: none;
            padding: 10px 20px;
            border-radius: 6px;
            transition: all 0.3s ease;
        }
        .btn-create:hover {
            background-color: #218838;
            transform: translateY(-2px);
            box-shadow: 0 4px 6px rgba(0,0,0,0.1);
        }
        .btn-edit {
            background-color: #ffc107;
            color: #212529;
        }
        .btn-delete {
            background-color: #dc3545;
            color: white;
        }
        .badge-featured {
            background-color: #17a2b8;
            color: white;
            padding: 5px 10px;
            border-radius: 4px;
        }
    </style>
</head>
<body>
    <div class="container">
        <div class="row mb-4 align-items-center">
            <div class="col">
                <h2 class="mb-0">Product Management</h2>
            </div>
            <div class="col-auto">
                <a href="/admin/products/create" class="btn btn-create">
                    <i class="fas fa-plus me-2"></i>Create New Product
                </a>
            </div>
        </div>

        <table class="table table-hover">
            <thead>
                <tr>
                    <th>ID</th>
                    <th>Title</th>
                    <th>Description</th>
                    <th>Price</th>
                    <th>Image</th>
                    <th>Featured</th>
                    <th>Actions</th>
                </tr>
            </thead>
            <tbody>
                <% for(let i = 0; i < products.length; i++) { %>
                <tr>
                    <td><%= products[i]._id %></td>
                    <td><%= products[i].title %></td>
                    <td><%= products[i].description %></td>
                    <td>₹<%= products[i].price.toFixed(2) %></td>
                    <td><img src="<%= products[i].imageSrc %>" alt="Image" style="width:50px;height:auto;"></td>
                    <td>
                        <% if(products[i].isFeatured) { %>
                            <span class="badge badge-featured">Featured</span>
                        <% } %>
                    </td>
                    <td>
                        <a href="/admin/products/edit/<%= products[i]._id %>" class="btn btn-sm btn-edit me-2">Edit</a>
                        <a href="/admin/products/delete/<%= products[i]._id %>" class="btn btn-sm btn-delete">Delete</a>
                    </td>
                </tr>
                <% } %>
            </tbody>
        </table>
    </div>
</body>
</html>
